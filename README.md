# nitinog10/Campus-Os — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

The `repo_02c61bb656d1` repository is a full-stack web application designed to facilitate the creation and management of digital assets using AI-driven tools. The project leverages a modern tech stack including React for the frontend, Node.js for the backend, and TypeScript for type safety. The application is built using Vite as the build tool, with Tailwind CSS for styling and PostCSS for additional CSS processing.

The frontend is structured with a clear component-based architecture, encapsulating UI elements and business logic within dedicated files and folders. Key components such as `AssetCard.tsx`, `Header.tsx`, and various UI elements are defined in the `src/components` directory. The application routes are managed within the `src/pages` folder, with pages like `Home.tsx` and `Create.tsx` handling user navigation.

On the backend, the server logic is contained within the `server` directory. Critical files include `server.js` for initializing the server, and route handlers such as `asset.js` and `generate.js` for managing API endpoints. The `lib` folder within the server contains utility functions, notably `openai.js` for integrating with OpenAI services.

This project is intended for users who need to generate and manage digital content efficiently, leveraging AI to automate parts of the creation process. The documentation and configuration files like `README.md`, `package.json`, and various `tsconfig` files ensure that the project is well-documented and easily maintainable.

## Architecture

```markdown
## Architecture Overview

### Code Organization

The CampusOS project is structured into several key layers and modules to ensure a clear separation of concerns and maintainability. Here's a breakdown of the main components:

#### Root Level
- **README.md**: Provides an overview and documentation for the project.
- **index.html**: The entry point for the web application.
- **package.json**: Manages project dependencies and scripts.
- **postcss.config.js**: Configures PostCSS plugins.
- **tailwind.config.ts**: Configures Tailwind CSS.
- **tsconfig.*.json**: Various TypeScript configuration files.
- **vite.config.ts**: Configures the Vite build tool.

#### Public Assets
- **public/**: Contains static assets like `vite.svg`.

#### Server
- **server/**: Houses the backend logic.
  - **.env.example**: Template for environment variables.
  - **package.json**: Manages server-side dependencies.
  - **server.js**: The main server entry point.
  - **lib/**: Utility libraries (e.g., `openai.js`).
  - **routes/**: Defines API routes (e.g., `asset.js`, `generate.js`).

#### Frontend
- **src/**: Contains the React frontend code.
  - **App.tsx**: Main application component.
  - **index.css**: Global styles.
  - **main.tsx**: Entry point for the React application.
  - **vite-env.d.ts**: TypeScript declarations for Vite.
  - **components/**: Reusable UI components.
    - **ui/**: Basic UI elements (e.g., `button.tsx`, `card.tsx`).
  - **events/**: Event-related components and logic.
  - **hooks/**: Custom React hooks (e.g., `useCreationEngine.ts`).
  - **lib/**: Utility functions (e.g., `utils.ts`).
  - **pages/**: Page components (e.g., `Home.tsx`, `Create.tsx`).
  - **prompt-intelligence/**: Prompt-related logic and components.
  - **services/**: API services (e.g., `api.ts`).
  - **types/**: TypeScript type definitions (e.g., `campusos.ts`).

### Data Flow

1. **User Interaction**: Users interact with the frontend via components like `Header.tsx` and `IntentInput.tsx`.
2. **Event Handling**: User actions trigger events managed by hooks like `useEventGenerator.ts`.
3. **API Requests**: The frontend sends requests to the backend via services like `api.ts`.
4. **Backend Processing**: The server processes requests in routes like `generate.js` and `asset.js`.
5. **Response Handling**: The server responds with data, which is then handled by the frontend.
6. **State Management**: The application state is managed using stores like `eventStore.ts`.

### Key Design Patterns

- **Component-Based Architecture**: The frontend is built using reusable React components.
- **Service Layer**: API interactions are encapsulated in service files like `api.ts`.
- **Hooks for Logic**: Custom hooks like `useCreationEngine.ts` manage complex logic.
- **Route-Based API**: The backend uses route files to handle different API endpoints.

### Main Entry Points

- **Frontend**: `index.html` and `main.tsx`.
- **Backend**: `server.js`.

This structure allows for clear separation of frontend and backend concerns, making the codebase easier to navigate and maintain.
```

## Folder Structure

```
├── README.md
├── index.html
├── package.json
├── postcss.config.js
├── tailwind.config.ts
├── tsconfig.app.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── docs/
│   └── screenshots/
│       ├── create-page.png
│       ├── history-page.png
│       └── homepage.png
├── public/
│   └── vite.svg
├── server/
│   ├── .env.example
│   ├── package.json
│   ├── server.js
│   ├── lib/
│   │   └── openai.js
│   └── routes/
│       ├── asset.js
│       ├── generate.js
│       ├── interpret.js
│       └── pipeline.js
└── src/
    ├── App.tsx
    ├── index.css
    ├── main.tsx
    ├── vite-env.d.ts
    ├── components/
    │   ├── AssetCard.tsx
    │   ├── Header.tsx
    │   ├── IntentInput.tsx
    │   ├── IntentSummary.tsx
    │   ├── PipelineView.tsx
    │   └── ui/
    │       ├── badge.tsx
    │       ├── button.tsx
    │       ├── card.tsx
    │       ├── progress.tsx
    │       ├── scroll-area.tsx
    │       ├── separator.tsx
    │       ├── skeleton.tsx
    │       ├── textarea.tsx
    │       └── toaster.tsx
    ├── events/
    │   ├── CreateEventModal.tsx
    │   ├── EventAssetGroup.tsx
    │   ├── eventStore.ts
    │   ├── index.ts
    │   └── useEventGenerator.ts
    ├── hooks/
    │   └── useCreationEngine.ts
    ├── lib/
    │   └── utils.ts
    ├── pages/
    │   ├── Create.tsx
    │   ├── History.tsx
    │   ├── Home.tsx
    │   ├── NotFound.tsx
    │   └── viewers/
    │       ├── LandingPageViewer.tsx
    │       ├── PosterViewer.tsx
    │       └── PresentationViewer.tsx
    ├── prompt-intelligence/
    │   ├── PromptTemplateSelector.tsx
    │   ├── buildPrompt.ts
    │   ├── index.ts
    │   └── promptTemplates.ts
    ├── services/
    │   ├── api.ts
    │   └── assetRegistry.ts
    └── types/
        └── campusos.ts
```

## Dependencies

### Dependencies

#### Production Dependencies

- **@radix-ui/react-dialog**: Provides accessible and unstyled dialog components. **Version**: ^1.1.14
- **@radix-ui/react-label**: Provides accessible and unstyled label components. **Version**: ^2.1.7
- **@radix-ui/react-progress**: Provides accessible and unstyled progress components. **Version**: ^1.1.7
- **@radix-ui/react-scroll-area**: Provides accessible and unstyled scroll area components. **Version**: ^1.2.9
- **@radix-ui/react-separator**: Provides accessible and unstyled separator components. **Version**: ^1.1.7
- **@radix-ui/react-slot**: Provides a way to render children as a slot. **Version**: ^1.2.3
- **@radix-ui/react-tabs**: Provides accessible and unstyled tabs components. **Version**: ^1.1.12
- **@radix-ui/react-toast**: Provides accessible and unstyled toast components. **Version**: ^1.2.14
- **@radix-ui/react-tooltip**: Provides accessible and unstyled tooltip components. **Version**: ^1.2.7
- **class-variance-authority**: Utility for managing class names conditionally. **Version**: ^0.7.1
- **clsx**: Utility for constructing className strings conditionally. **Version**: ^2.1.1
- **framer-motion**: Library for production-ready micro-animations. **Version**: ^11.18.0
- **html2canvas**: JavaScript library to convert HTML to canvas. **Version**: ^1.4.1
- **jspdf**: Library to generate PDFs in JavaScript. **Version**: ^4.1.0
- **lucide-react**: Set of React icons based on Lucide. **Version**: ^0.462.0
- **react**: The core library for building user interfaces. **Version**: ^18.3.1
- **react-dom**: The library for rendering React components to the DOM. **Version**: ^18.3.1
- **react-markdown**: Markdown component for React. **Version**: ^10.1.0
- **react-router-dom**: DOM bindings for React Router. **Version**: ^6.30.1
- **sonner**: Library for notifications. **Version**: ^1.7.4
- **tailwind-merge**: Utility for merging Tailwind CSS class names. **Version**: ^2.6.0
- **tailwindcss-animate**: Tailwind CSS plugin for animations. **Version**: ^1.0.7

#### Development Dependencies

- **@types/node**: TypeScript definitions for Node.js. **Version**: ^22.16.5
- **@types/react**: TypeScript definitions for React. **Version**: ^18.3.23
- **@types/react-dom**: TypeScript definitions for React DOM. **Version**: ^18.3.7
- **@vitejs/plugin-react-swc**: Vite plugin for React with SWC compiler. **Version**: ^3.11.0
- **autoprefixer**: PostCSS plugin to add vendor prefixes. **Version**: ^10.4.21
- **concurrently**: Command runner to execute multiple commands concurrently. **Version**: ^9.1.0
- **postcss**: Tool for transforming CSS with JavaScript plugins. **Version**: ^8.5.6
- **tailwindcss**: Utility-first CSS framework for rapid UI development. **Version**: ^3.4.17
- **typescript**: Superset of JavaScript that adds static types. **Version**: ^5.8.3
- **vite**: Next-generation frontend tooling. **Version**: ^5.4.19

## File Documentation

### README.md

#### Overview

# Module Overview

The `README.md` file serves as the primary documentation for the CampusOS project, providing an overview of the platform, its features, architecture, and how it works. It's the first point of contact for anyone looking to understand or contribute to the project.

#### Dependencies

- **React**: Core library for building the user interface.
- **Vite**: Build tool that significantly improves the development experience with fast hot module replacement.
- **TailwindCSS**: Utility-first CSS framework for rapidly building custom designs.
- **Framer Motion**: Library for adding animations and gestures to React components.
- **Express.js**: Backend framework for handling API requests.
- **OpenAI API**: Provides the AI capabilities for generating digital assets.

#### Classes

None. This file is markdown and does not contain any classes.

#### Functions

None. This file is markdown and does not contain any functions.

#### Configuration

None. This file does not require any specific configuration.

#### Constants

None. This file does not define any constants.

#### Notes

- The `README.md` file uses Mermaid diagrams to visually explain the architecture and workflows. Ensure you have the necessary plugins enabled in your markdown viewer to see these diagrams.
- The project structure and architecture sections provide a high-level overview but dive into specific components and their interactions.
- The features section details the unique capabilities of CampusOS, including the Guided Prompt mode and Campus Event Mode.
- The how it works section breaks down the step-by-step process of generating assets, both for single assets and in Campus Event Mode.
- Screenshots are provided to give a visual representation of key pages in the application.

### index.html

#### Overview

# index.html

This file serves as the entry point for the CampusOS web application. It sets up the basic HTML structure and includes essential meta tags, stylesheets, and scripts needed for the application to function correctly.

#### Dependencies

- **Vite SVG icon**: `/vite.svg` - Used as the application icon.
- **Google Fonts**: `https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap` - Provides a variety of font weights for the application.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration or Constants

- **Dark Mode**: The `html` tag has a `class="dark"` attribute indicating that the application is initialized in dark mode.

#### Notes

- The `<div id="root"></div>` element is where the React application will be mounted.
- The main application logic is loaded via the `/src/main.tsx` script.
- The meta tags provide character encoding, viewport settings, and a description for SEO purposes.

### package.json

#### Overview

# `package.json` Documentation

The `package.json` file is the heart of any Node.js project. It holds metadata about the project, including its dependencies, scripts, and versioning information. This file is essential for managing project dependencies and defining build scripts.

#### Dependencies

| Dependency | Purpose |
| --- | --- |
| `@radix-ui/react-dialog` | Provides accessible UI components. |
| `@radix-ui/react-label` | Enhances form elements with better accessibility. |
| `@radix-ui/react-progress` | Displays progress bars. |
| `@radix-ui/react-scroll-area` | Adds scrollable areas to components. |
| `@radix-ui/react-separator` | Adds visual separation between elements. |
| `@radix-ui/react-slot` | Allows for custom component rendering. |
| `@radix-ui/react-tabs` | Implements tabbed navigation. |
| `@radix-ui/react-toast` | Displays toast notifications. |
| `@radix-ui/react-tooltip` | Adds tooltips to elements. |
| `class-variance-authority` | Manages conditional class names. |
| `clsx` | Utility for constructing class names conditionally. |
| `framer-motion` | Provides animations for React components. |
| `html2canvas` | Converts HTML elements to canvas. |
| `jspdf` | Generates PDF documents. |
| `lucide-react` | Provides React icons. |
| `react` | The core React library. |
| `react-dom` | Provides DOM manipulation for React. |
| `react-markdown` | Renders markdown as React components. |
| `react-router-dom` | Adds routing capabilities to React applications. |
| `sonner` | Enhances audio capabilities. |
| `tailwind-merge` | Merges Tailwind CSS classes. |
| `tailwindcss-animate` | Adds animations to Tailwind CSS. |

#### DevDependencies

| Dependency | Purpose |
| --- | --- |
| `@types/node` | TypeScript definitions for Node.js. |
| `@types/react` | TypeScript definitions for React. |
| `@types/react-dom` | TypeScript definitions for React DOM. |
| `@vitejs/plugin-react-swc` | Vite plugin for React with SWC compiler. |
| `autoprefixer` | Adds vendor prefixes to CSS. |
| `concurrently` | Runs multiple commands concurrently. |
| `postcss` | Processes CSS with JavaScript plugins. |
| `tailwindcss` | Utility-first CSS framework. |
| `typescript` | TypeScript compiler. |
| `vite` | Fast build tool and development server. |

#### Scripts

| Script | Command |
| --- | --- |
| `dev` | Runs the development server with Vite. |
| `build` | Compiles TypeScript and builds the project with Vite. |
| `preview` | Previews the production build. |
| `lint` | Runs ESLint to check for code quality. |
| `server` | Starts the backend server. |
| `dev:all` | Runs both the development server and backend server concurrently. |

#### Notes

- Ensure all dependencies are up-to-date to avoid compatibility issues.
- The `dev:all` script is useful for full-stack development, running both frontend and backend servers simultaneously.
- When adding new dependencies, consider their impact on bundle size and performance.

### postcss.config.js

#### Overview

# postcss.config.js

This file configures PostCSS plugins for our project. It specifies which PostCSS plugins to use to process our CSS.

#### Dependencies

- `tailwindcss`: Integrates Tailwind CSS for utility-first styling.
- `autoprefixer`: Automatically adds vendor prefixes to CSS rules using values from [Can I Use](https://caniuse.com).

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `export default` | None | Configuration object | Returns the configuration object for PostCSS plugins |

#### Configuration

- `plugins`: An object containing the PostCSS plugins to use.

#### Notes

- Ensure that the PostCSS plugins are installed as dependencies in `package.json`.
- Tailwind CSS and Autoprefixer are the only plugins currently configured. Additional plugins can be added here if needed.

### tailwind.config.ts

#### Overview

# `tailwind.config.ts`

This file configures Tailwind CSS for the project, defining custom themes, animations, and plugins.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Config` from `tailwindcss` | Type definition for Tailwind configuration. |
| `tailwindcssAnimate` from `tailwindcss-animate` | Plugin for adding animations to Tailwind CSS. |

#### Configuration

| Key | Value | Description |
| --- | --- | --- |
| `darkMode` | `"class"` | Enables dark mode using a class. |
| `content` | `["./index.html", "./src/**/*.{ts,tsx}"]` | Specifies the paths to all of your template files. |
| `theme.extend` | Custom theme settings | Extends the default Tailwind theme with custom fonts, colors, border radii, keyframes, and animations. |
| `plugins` | `[tailwindcssAnimate]` | Adds the `tailwindcssAnimate` plugin to enable animations. |

#### Notes

- The `hsl(var(--*))` syntax is used for colors, which allows for dynamic theming based on CSS variables.
- Custom animations are defined in the `keyframes` and `animation` sections.
- The `borderRadius` values are calculated based on a CSS variable `--radius`.
- Ensure that the CSS variables used in the configuration are defined in your stylesheets.

### tsconfig.app.json

#### Overview

# tsconfig.app.json

This file configures the TypeScript compiler for our application. It sets compiler options that dictate how TypeScript code is compiled, ensuring compatibility with modern JavaScript standards and enabling advanced TypeScript features.

#### Dependencies

- **TypeScript**: The primary dependency, used to compile TypeScript code into JavaScript.

#### Configuration

| Option            | Purpose                  | Details                                                                                           |
|-------------------|--------------------------|---------------------------------------------------------------------------------------------------|
| `target`          | JavaScript version       | Set to `ES2020` to compile to ES2020 JavaScript standard.                                           |
| `useDefineForClassFields` | Class fields syntax     | Enables the use of `#` for class fields.                                                            |
| `lib`             | Standard libraries       | Includes `ES2020`, `DOM`, and `DOM.Iterable` libraries.                                            |
| `module`          | Module system           | Set to `ESNext` to use the latest ECMAScript module system.                                        |
| `skipLibCheck`    | Compiler optimization   | Skips type checking of all declaration files (`.d.ts`).                                           |
| `moduleResolution`| Module resolution        | Uses the bundler for module resolution.                                                           |
| `allowImportingTsExtensions` | TypeScript extensions | Allows importing of TypeScript declaration files.                                                |
| `isolatedModules` | Module isolation        | Ensures each file is treated as a separate module, preventing variable name collisions.           |
| `moduleDetection` | Module detection        | Forces module detection to always use the module system specified in `module`.                      |
| `noEmit`          | Compilation output      | Prevents emitting compiled JavaScript files, useful for type checking only.                        |
| `jsx`             | JSX factory             | Sets the JSX factory to `react-jsx` for React.                                                      |
| `strict`          | Type checking           | Enables all strict type-checking options.                                                         |
| `baseUrl`         | Module resolution base  | Sets the base URL for non-relative module names.                                                   |
| `paths`           | Module path mapping     | Maps `@/*` to `./src/*` for convenient module imports.                                             |

#### Notes

- The `noEmit` option is set to `true`, meaning this configuration is primarily for type checking and not for actual compilation.
- The `paths` configuration under `baseUrl` simplifies imports by allowing `@/` to refer to the `src` directory.
- The `strict` option is enabled to enforce strict type checking, which helps catch errors early in the development process.

### tsconfig.json

#### Overview

# tsconfig.json

This file consolidates TypeScript configuration settings for the project, including references to other configuration files that apply to different environments.

#### Dependencies

- `tsconfig.app.json`: Configuration for the application-specific TypeScript settings.
- `tsconfig.node.json`: Configuration for TypeScript settings when running in a Node.js environment.

#### Configuration

| Configuration | Purpose |
|---------------|---------|
| `files` | An empty array indicating no direct files are included here. |
| `references` | An array of paths to other `tsconfig` files that provide environment-specific configurations. |

#### Notes

- This file is used to merge multiple `tsconfig` files, allowing for environment-specific TypeScript configurations to be managed cleanly.
- Ensure that the paths in the `references` array are correct and point to the appropriate `tsconfig` files for your environment.

### tsconfig.node.json

#### Overview

# tsconfig.node.json

This file configures the TypeScript compiler for Node.js-specific settings. It defines compiler options that dictate how TypeScript code is compiled, ensuring compatibility with Node.js and adhering to strict type-checking rules.

#### Dependencies

This file doesn't directly import any modules but relies on TypeScript's built-in compiler options and configuration capabilities.

#### Configuration

| Setting | Purpose |
| --- | --- |
| `target` | Sets the ECMAScript target version (`ES2022`). |
| `lib` | Includes standard library type definitions (`ES2023`). |
| `module` | Specifies the module code generation (`ESNext`). |
| `skipLibCheck` | Skips type checking of all declaration files (`dts`). |
| `moduleResolution` | Uses the bundler for module resolution. |
| `allowImportingTsExtensions` | Allows importing TypeScript declaration files. |
| `isolatedModules` | Treats each file as a separate module. |
| `moduleDetection` | Forces module detection. |
| `noEmit` | Prevents emitting output files. |
| `strict` | Enables all strict type-checking options. |
| `noUnusedLocals` | Reports errors on unused local variables. |
| `noUnusedParameters` | Reports errors on unused function parameters. |
| `noFallthroughCasesInSwitch` | Reports errors for switch cases without break statements. |

#### Notes

- `noEmit` is set to `true` to prevent the compiler from generating JavaScript files, which is useful when using a bundler like Vite.
- `strict` mode is enabled to enforce strict type-checking, which helps catch errors early in the development process.
- `isolatedModules` ensures that each file is treated as a separate module, which can help with tree-shaking and optimize bundle sizes.

### vite.config.ts

#### Overview

# `vite.config.ts`

This file configures the Vite build tool for our project. It sets up plugins, resolves module paths, and proxies API requests during development.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `defineConfig` | Function to define Vite configuration. |
| `react` | Plugin to integrate React with Vite. |
| `path` | Module for handling and transforming file paths. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `defineConfig` | `config` | Vite configuration object | Main function to define the Vite configuration. |

#### Configuration

| Option | Purpose |
| --- | --- |
| `plugins` | Specifies plugins to use in the build process. |
| `resolve.alias` | Maps alias `@` to the `src` directory for easier imports. |
| `server.proxy` | Proxies API requests to `http://localhost:3001` during development. |

#### Notes

- The `react` plugin is essential for using React with Vite.
- The `resolve.alias` helps to simplify import paths by aliasing `@` to the `src` directory.
- The `server.proxy` configuration is crucial for local development, allowing us to proxy API requests to a different port.

### server/.env.example

#### Overview

# server/.env.example

**Module Overview**

This file serves as a template for environment variables required by the server. It provides placeholders for sensitive and configuration settings that should be set in a `.env` file in the root directory of the project.

**Dependencies**

No direct dependencies are listed here, but it relies on the `dotenv` package to load environment variables from the `.env` file into the application.

**Configuration**

| Configuration | Purpose | 
|---------------|---------|
| `OPENAI_API_KEY` | API key for accessing the OpenAI service. |

**Notes**

- Always keep the `.env` file secure and out of version control.
- Ensure sensitive information like API keys are not hard-coded in the source code.
- The actual `.env` file should be created from this example and placed in the root directory of the project.

### server/package.json

#### Overview

# server/package.json

#### Module Overview

This file defines the dependencies and scripts for our server application. It ensures that all required packages are installed and specifies how to run the server in both production and development environments.

#### Dependencies

| Package | Purpose |
| --- | --- |
| `cors` | Enables Cross-Origin Resource Sharing for handling requests from different origins. |
| `dotenv` | Loads environment variables from a `.env` file into `process.env`. |
| `express` | A minimal and flexible Node.js web application framework that provides a robust set of features for web and mobile applications. |
| `openai` | Provides an interface to interact with the Open Assistant API. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | No functions defined in this file. |

#### Notes

- The `start` script runs the server using `server.js`.
- The `dev` script runs the server in watch mode for development purposes.
- Ensure environment variables are set in the `.env` file for proper configuration.

### server/server.js

#### Overview

# server/server.js

#### Module Overview

This file sets up and runs the main server for the CampusOS API. It initializes an Express application, configures middleware, and mounts various routers for different API endpoints. It also includes a health check endpoint to verify the server's status.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `dotenv/config` | Loads environment variables from a `.env` file. |
| `express` | Web framework for building the API server. |
| `cors` | Middleware to handle Cross-Origin Resource Sharing. |
| `interpretRouter` | Router for interpret-related endpoints. |
| `pipelineRouter` | Router for pipeline-related endpoints. |
| `assetRouter` | Router for asset-related endpoints. |
| `generateRouter` | Router for generate-related endpoints. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `app` | Express application instance. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `app.listen` | `PORT` | N/A | Starts the server and logs the URL. |

#### Configuration

| Variable | Purpose |
| --- | --- |
| `PORT` | The port number the server listens on, defaults to 3001 if not set in environment variables. |

#### Notes

- The server uses CORS to allow requests from `http://localhost:5173` and `http://localhost:4173`.
- The `/api` path is consolidated for new endpoints, but legacy endpoints are still mounted for backwards compatibility.
- A health check endpoint is available at `/api/health` to verify the server's status.
- The server logs its URL and health check URL upon starting.

### server/lib/openai.js

#### Overview

# server/lib/openai.js

#### Module Overview

This file initializes and configures the `OpenAI` client for interacting with the OpenAPI service. It reads the API key from environment variables and sets up the client to make API requests.

#### Dependencies

| Dependency | Purpose |
|------------|---------|
| `dotenv`   | Loads environment variables from a `.env` file. |
| `openai`   | Official client for interacting with the OpenAPI service. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `OpenAI` | Client for OpenAPI service | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `openai` | N/A | `OpenAI` instance | Initialized client for OpenAPI service |

#### Configuration

This file relies on the `OPENAI_API_KEY` environment variable to authenticate API requests.

#### Notes

- Ensure the `OPENAI_API_KEY` is set in the `.env` file to avoid warnings and failed API calls.
- The placeholder key `"sk-placeholder-set-your-key"` is used if the actual API key is not provided. Replace it with your actual key.

### server/routes/asset.js

#### Overview

### Module Overview

The `server/routes/asset.js` file defines the routes for generating various types of content (text, design, code) using the `openai` library. It utilizes the Express Router to handle HTTP POST requests to the `/generate-asset` endpoint, which triggers the generation of content based on the provided step and intent.

### Dependencies

| Import | Description |
| --- | --- |
| `Router` from `express` | Provides a way to create modular, mountable route handlers. |
| `openai` from `../lib/openai.js` | A custom wrapper for interacting with the OpenAI API. |
| `cleanCodeOutput` from `../utils/cleaners.js` | A utility function to clean up the generated code output. |

### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `assetRouter.post("/generate-asset",...)` | `req`, `res` | HTTP response | Handles POST requests to generate content. Requires `step` and `intent` in the request body. |

### Configuration

| Variable | Description |
| --- | --- |
| `contentTypeMap` | Maps step types to content types. |
| `systemPrompts` | Contains system prompts for different step types (text, design, code). |

### Notes

- The `openai.chat.completions.create` method is used to generate content. Ensure the `openai` library is correctly configured with API keys and endpoints.
- The `cleanCodeOutput` function is used to sanitize and format the generated content, especially for code outputs.
- Error handling is implemented to catch and respond to any issues during the content generation process.
- The system prompts are designed to guide the model in generating specific types of content based on the provided step and intent.
- The response includes a detailed explanation of the generated content for better traceability and debugging.

### server/routes/generate.js

#### Overview

# **server/routes/generate.js**

#### Module Overview

This file defines an Express router for generating HTML content using the `openai` API. It provides endpoints to create high-quality, visually stunning posters and landing pages.

#### Dependencies

- `Router` from `express`: Handles routing.
- `openai` from `../lib/openai.js`: Interface for interacting with the OpenAI API.
- `crypto`: Node.js crypto module for generating unique IDs.

#### Configuration

- `MODEL`: Defaults to `"gpt-4o"`, can be overridden by `process.env.OPENAI_MODEL`.

#### Constants

- `IMAGE_INSTRUCTIONS`: Detailed instructions for including high-quality images in generated content.
- `ANIMATION_INSTRUCTIONS`: CSS animations and application rules for dynamic effects.
- `POSTER_PROMPT`: Prompt template for generating event posters.
- `LANDING_PAGE_PROMPT`: Prompt template for generating landing pages.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `generateRouter` | N/A | `Router` | Initializes and returns the Express router for generation endpoints. |

#### Notes

- Ensure environment variables are correctly set for `OPENAI_MODEL`.
- The generated HTML content is expected to be raw and self-contained.
- The `IMAGE_INSTRUCTIONS` and `ANIMATION_INSTRUCTIONS` are critical for maintaining high-quality standards in the generated content.

### server/routes/interpret.js

#### Overview

# server/routes/interpret.js

#### Module Overview

This file defines the `interpretRouter`, an Express router that handles the `/interpret-intent` endpoint. It uses the `openai` library to process natural language input from users and interpret their intent into a structured format.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `Router` from `express` | Provides the Express Router to define routes. |
| `openai` from `../lib/openai.js` | Interface to the OpenAI API for generating responses. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `interpretRouter.post("/interpret-intent",...)` | `req`, `res` | `JSON` | Handles POST requests to interpret user intent from the provided `prompt`. |

#### Notes

- The endpoint expects a `prompt` in the request body. If not provided, it returns a 400 status with an error message.
- The `openai.chat.completions.create` function is used to generate a structured intent based on the user's input.
- The response from the model is expected to be a JSON object with specific fields. If the response is not in the expected format, an error is thrown.
- Errors during the process are logged and a 500 status is returned with an appropriate error message.

### server/routes/pipeline.js

#### Overview

# server/routes/pipeline.js

#### Module Overview

This file defines the routes for handling pipeline generation requests. It uses the Express Router to create a `/generate-pipeline` endpoint that interacts with an external AI model to create a sequence of steps for executing a given intent.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router` from `express` | To create a new router instance for handling HTTP requests |
| `openai` from `../lib/openai.js` | To interact with the external AI model for generating pipeline steps |
| `crypto` | To generate unique IDs for the pipeline and its steps |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `pipelineRouter.post("/generate-pipeline",...)` | `req`, `res` | JSON response | Handles POST requests to `/generate-pipeline`, generates a pipeline based on the provided intent, and returns the pipeline JSON |

#### Notes

- The `openai.chat.completions.create` function call is configured to use the `gpt-4o-mini` model with a temperature of 0.3.
- The response from the AI model is expected to be a JSON object with a specific structure.
- If the `intent` is missing from the request body, a 400 status code is returned.
- The pipeline and its steps are assigned unique IDs using the `crypto.randomUUID` function.
- Any errors during the pipeline generation process result in a 500 status code response.

### src/App.tsx

#### Overview

# Module Overview

`src/App.tsx` is the main entry point for our React application. It sets up the routing structure and defines the layout for the application. It includes the header for most pages and handles routing for both layout-based and viewer-based pages.

# Dependencies

| Import | Source | Purpose |
| --- | --- | --- |
| `BrowserRouter`, `Routes`, `Route` | `react-router-dom` | Provides routing capabilities for the application. |
| `Toaster` | `@/components/ui/toaster` | Displays toast notifications throughout the app. |
| `Header` | `@/components/Header` | Displays the application header on layout pages. |
| `Home`, `Create`, `History`, `NotFound` | `@/pages` | Main application pages with layout. |
| `PosterViewer`, `LandingPageViewer`, `PresentationViewer` | `@/pages/viewers` | Full-screen viewer pages without a header. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `AppLayout` | Wraps children with the application header | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `App` | None | JSX elements | Main function rendering the application routes and layout. |

# Configuration or Constants

None

# Notes

- `AppLayout` is used to wrap layout pages and include the header.
- Viewer pages (`PosterViewer`, `LandingPageViewer`, `PresentationViewer`) do not include the header.
- The `Toaster` component is always rendered at the bottom of the application.
- The `Routes` component defines the paths and corresponding components for the application.

### src/index.css

#### Overview

# src/index.css

#### Module Overview

This file contains global CSS styles, including Tailwind CSS imports, custom properties, and various utility classes for the CampusOS design system.

#### Dependencies

- `@tailwind base`
- `@tailwind components`
- `@tailwind utilities`

#### Classes

| Class | Purpose | Key Properties |
| --- | --- | --- |
| `.noise-overlay::before` | Adds a noise texture overlay | `background-image`, `opacity` |
| `.glass` | Applies glassmorphism effect | `backdrop-filter`, `border` |
| `.glass-strong` | Stronger glassmorphism effect | `backdrop-filter`, `border` |
| `.glass-card` | Glassmorphism effect with shadow | `backdrop-filter`, `border`, `box-shadow` |
| `.gradient-text` | Applies gradient text effect | `background`, `background-clip`, `-webkit-text-fill-color` |
| `.gradient-text-warm` | Warm gradient text effect | `background`, `background-clip`, `-webkit-text-fill-color` |
| `.gradient-border` | Applies gradient border effect | `position`, `border-radius` |
| `.gradient-border::before` | Gradient border pseudo-element | `background`, `mask`, `mask-composite` |
| `.animated-border` | Container for animated gradient border | `position`, `overflow` |
| `.animated-border::before` | Animated gradient border pseudo-element | `background`, `mask`, `mask-composite`, `animation` |
| `.glow-purple` | Purple glow effect | `box-shadow` |
| `.glow-blue` | Blue glow effect | `box-shadow` |
| `.glow-multi` | Multi-color glow effect | `box-shadow` |
| `.card-depth` | Card depth effect | `box-shadow`, `transition` |
| `.card-depth:hover` | Hover effect for card depth | `box-shadow`, `transform` |
| `.bg-grid` | Background grid pattern | `background-image`, `background-size` |
| `.bg-dot-pattern` | Background dot pattern | `background-image`, `background-size` |
| `.aurora-orb` | Aurora orb base style | `position`, `border-radius`, `filter`, `opacity`, `animation` |
| `.aurora-orb-1` | Aurora orb 1 style | `width`, `height`, `background`, `top`, `left`, `animation-delay` |
| `.aurora-orb-2` | Aurora orb 2 style | `width`, `height`, `background`, `bottom`, `right`, `animation-delay`, `animation-duration` |
| `.aurora-orb-3` | Aurora orb 3 style | `width`, `height`, `background`, `top`, `left`, `animation-delay`, `animation-duration` |
| `.shimmer` | Shimmer effect | `background`, `background-size`, `animation` |
| `.shimmer-border` | Shimmer border effect | `position`, `overflow` |
| `.shimmer-border::after` | Shimmer border pseudo-element | `background`, `background-size`, `mask`, `mask-composite`, `animation` |
| `.animate-pulse-glow` | Pulse glow animation | `animation` |
| `.animate-float` | Float animation | `animation` |
| `.animate-scale-in` | Scale-in animation | `animation` |
| `.animate-slide-up` | Slide-up fade animation | `animation` |
| `.animate-gradient-sweep` | Gradient sweep animation | `background-size`, `animation` |
| `.orbit-ring` | Orbital ring animation | `animation`, `border-radius`, `border` |
| `.orbit-ring-reverse` | Reverse orbital ring animation | `animation`, `border-radius`, `border` |
| `.scan-line` | Scanning line animation | `animation`, `background` |
| `.gradient-line` | Gradient line for headers/dividers | `height`, `background` |
| `.accent-bar` | Accent bar base style | `position` |
| `.accent-bar::before` | Accent bar pseudo-element | `background`, `border-radius` |
| `.accent-bar-pink::before` | Pink accent bar | `background` |
| `.accent-bar-blue::before` | Blue accent bar | `background` |
| `.accent-bar-amber::before` | Amber accent bar | `background` |
| `.hover-tilt` | Hover tilt effect | `transform-style`, `transition` |
| `.hover-tilt:hover` | Hover tilt effect on hover | `transform` |

#### Notes

- Ensure to use Tailwind CSS classes where possible to maintain consistency.
- Custom properties defined in `:root` and `.dark` are used throughout the application for theming.
- Animations and keyframes are used extensively for visual effects; ensure they are performant.
- The `.aurora-orb` classes create a dynamic aurora effect; adjust properties as needed for different designs.
- The `.shimmer` and `.shimmer-border` classes provide a loading state effect; use them for elements that are loading data.
- The `.animate-*` classes apply various animations; use them sparingly to avoid overwhelming the user.

### src/main.tsx

#### Overview

# Module Overview

This file is the entry point for our React application. It initializes the root React component and renders it to the DOM.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Core React library for building UI components. |
| `ReactDOM` | Library for rendering React to the DOM and managing the root component. |
| `App` | Main application component that holds the application logic and structure. |
| `./index.css` | Global styles for the application. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ReactDOM.createRoot(document.getElementById("root")!).render` | `React.StrictMode` and `App` | None | Renders the `App` component inside a `React.StrictMode` wrapper to the DOM. |

#### Notes

- The `root` element must exist in the HTML file for this to work.
- `React.StrictMode` helps identify potential problems in the application.
- Ensure `index.css` is correctly linked in the HTML to apply global styles.

### src/vite-env.d.ts

#### Overview

# Module Overview

The `src/vite-env.d.ts` file provides TypeScript declaration types for Vite, allowing the TypeScript compiler to understand Vite-specific globals and types within the project.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `vite/client` | Provides type definitions for Vite's client-side environment. |

#### Notes

- This file ensures that TypeScript can correctly type-check any Vite-specific globals or utilities used in the project.
- It's crucial for maintaining type safety when working with Vite features in TypeScript.

### src/components/AssetCard.tsx

#### Overview

# AssetCard.tsx

#### Module Overview

This file defines the `AssetCard` component, which displays a single asset in a stylized card format. It supports viewing HTML content in a preview mode, displaying markdown content, or showing raw text. The card also includes actions like copying content to the clipboard, downloading the asset, and toggling between preview and code view for HTML content.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useState` from `react` | Manages local state within the component. |
| `motion` from `framer-motion` | Provides animation capabilities for the card. |
| Icons from `lucide-react` | Provides various icons used in the component. |
| Components from `@/components/ui` | Provides UI components like `Card`, `Button`, and `Badge`. |
| `ReactMarkdown` | Renders markdown content. |
| `toast` from `sonner` | Displays notifications. |
| `GeneratedAsset` type from `@/types/campusos` | Defines the type for the asset prop. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `AssetCard` | `{ asset: GeneratedAsset, index: number }` | JSX element | Renders the asset card with its content and actions. |

#### Notes

- The `handleCopy` function copies the asset's content to the clipboard and shows a success toast.
- The `handleDownload` function creates a downloadable link for the asset and shows a success toast.
- The component uses conditional rendering to display the asset's content based on its content type.
- Animations are applied to the card using `framer-motion`.

### src/components/Header.tsx

#### Overview

# src/components/Header.tsx

#### Module Overview

This file defines the `Header` component, which is the top navigation bar for the application. It includes the logo and navigation links with icons, and uses animations for a polished user experience.

#### Dependencies

| Import | From | Purpose |
| :----- | :--- | :------ |
| `Link` | `react-router-dom` | For navigation between routes |
| `useLocation` | `react-router-dom` | To get the current location |
| `motion` | `framer-motion` | For animations |
| `Cpu`, `Plus`, `Clock`, `Sparkles` | `lucide-react` | Icons for navigation |
| `Button` | `@/components/ui/button` | Pre-styled button component |

#### Functions

| Function | Parameters | Returns | Description |
| :------- | :--------- | :------ | :---------- |
| `Header` | None | JSX for the header component | Renders the header with navigation links and animations |

#### Notes

- The `Header` component uses `useLocation` to determine the active navigation link.
- The `motion.header` component from `framer-motion` provides smooth entrance animations.
- The `Button` component is conditionally styled based on whether the link is active.
- The icons used in the navigation are from `lucide-react` and are displayed conditionally based on the active state.

### src/components/IntentInput.tsx

#### Overview

### Module Overview

The `src/components/IntentInput.tsx` file defines a React component for an input field that allows users to submit prompts. It includes an animated text area, a submit button, and a set of example prompts to help users generate ideas.

### Dependencies

| Import | Description |
| --- | --- |
| `useState`, `useRef`, `useEffect` | React hooks for state management, refs, and side effects. |
| `motion`, `AnimatePresence` | Animation utilities from `framer-motion` for smooth transitions. |
| `Button`, `Textarea` | Custom UI components for button and textarea. |
| Icons from `lucide-react` | Various icons used for UI elements. |

### Classes

There are no classes defined in this file.

### Functions

There are no standalone functions defined in this file.

### Configuration

| Variable | Type | Description |
| --- | --- | --- |
| `examplePrompts` | Array of objects | A list of example prompts with labels, prompts, icons, and accent colors. |

### Notes

- The `handleSubmit` function checks if the prompt is not empty and if the component is not in a loading state before submitting.
- The `handleExampleClick` function sets the prompt text and hides the example prompts when an example is selected.
- The `handleKeyDown` function allows users to submit the form by pressing `Ctrl + Enter`.
- The `useEffect` hook focuses the textarea when the component mounts.
- The `AnimatePresence` component from `framer-motion` is used to animate the appearance and disappearance of example prompts.

### src/components/IntentSummary.tsx

#### Overview

# IntentSummary.tsx

#### Module Overview

This file defines the `IntentSummary` component, which displays a summary of an intent with its associated details and metadata. It uses a card layout to present the information in a visually appealing and organized manner.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `motion` from `framer-motion` | Provides animation capabilities for the component. |
| `Target`, `Users`, `Megaphone`, `Palette` from `lucide-react` | Provides icon components for different intent types. |
| `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card` | Provides UI components for the card layout. |
| `Badge` from `@/components/ui/badge` | Provides a badge component for displaying tags. |
| `ParsedIntent` from `@/types/campusos` | Defines the type for the intent data. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `IntentSummary` | `{ intent: ParsedIntent }` | JSX element | Renders the intent summary card with animations and details. |

#### Configuration

| Key | Description |
| --- | --- |
| `typeIcons` | A mapping of intent types to their corresponding icons. |
| `typeLabels` | A mapping of intent types to their human-readable labels. |

#### Notes

- The component uses conditional rendering to display icons and labels based on the intent type.
- The `motion.div` wrapper provides a fade-in and scale-up animation effect.
- Ensure that the `intent` prop passed to the component matches the `ParsedIntent` type for proper rendering.

### src/components/PipelineView.tsx

#### Overview

# PipelineView.tsx

#### Module Overview

This file defines the `PipelineView` component, which visualizes the status of a pipeline's execution steps. It uses a combination of icons, colors, and animations to provide a clear and engaging representation of the pipeline's progress.

#### Dependencies

| Import | Description |
| --- | --- |
| `motion` from `framer-motion` | Provides animation capabilities. |
| Icons from `lucide-react` | Provides various icons used to represent different step statuses and types. |
| `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card` | UI components for styling the pipeline view. |
| `Progress` from `@/components/ui/progress` | Component for displaying progress. |
| `Pipeline`, `StepStatus`, `StepType` from `@/types/campusos` | Types for the pipeline data. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `PipelineView` | `{ pipeline: Pipeline }` | JSX.Element | Renders the pipeline view with animated steps and progress. |

#### Configuration

| Key | Value |
| --- | --- |
| `statusConfig` | A mapping of step statuses to their corresponding icons and colors. |
| `typeIcons` | A mapping of step types to their corresponding icons. |

#### Notes

- The component uses `framer-motion` for animations, which adds a dynamic feel to the pipeline's steps.
- The progress bar updates based on the number of completed steps in the pipeline.
- Icons and colors change dynamically based on the status and type of each step.
- Ensure that the `Pipeline` type and `StepStatus`, `StepType` enums are correctly defined in the `@/types/campusos` file.

### src/components/ui/badge.tsx

#### Overview

# `src/components/ui/badge.tsx` Documentation

This file defines a reusable `Badge` component for displaying labels with different styles and variants.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | React library for building UI components. |
| `cva` | Utility from `class-variance-authority` for managing class variants. |
| `cn` | Utility from `@/lib/utils` for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `badgeVariants` | Defines the styles and variants for the badge component. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Badge` | `className`, `variant`, `...props` | `div` | Renders a badge with specified variant and optional className. |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `badgeVariants` | Configures different styles for the badge based on the `variant` prop. |

#### Notes

- The `variant` prop determines the style of the badge (`default`, `secondary`, `destructive`, `outline`, `glow`).
- The `cn` utility is used to merge the base badge styles with any additional className provided.
- The `badgeVariants` utility handles the application of different styles based on the `variant` prop.

### src/components/ui/button.tsx

#### Overview

# Module Overview

The `src/components/ui/button.tsx` file defines a customizable `Button` component for the application. It leverages Radix UI's `Slot` component for flexibility and `class-variance-authority` for managing class variants.

#### Dependencies

| Import | Description |
| --- | --- |
| `React` | Core React library for building UI components. |
| `Slot` from `@radix-ui/react-slot` | Allows the button to be rendered as any valid HTML element or component. |
| `cva` and `VariantProps` from `class-variance-authority` | Utilities for managing and applying conditional class variants. |
| `cn` from `@/lib/utils` | A utility function to concatenate class names conditionally. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Button` | The main button component, customizable via props. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration

- `buttonVariants`: Configures the button's appearance based on `variant` and `size` props.

#### Notes

- The `Button` component can be rendered as any HTML element or React component by setting the `asChild` prop to `true`.
- The `variant` and `size` props control the button's style and dimensions.
- The `className` prop can be used to add additional CSS classes to the button.
- The `ref` prop is forwarded to the underlying button or slot component, allowing for direct DOM access if needed.

### src/components/ui/card.tsx

#### Overview

# `src/components/ui/card.tsx`

This file defines a set of reusable card components for our UI, each with specific roles and styles.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Provides the building blocks for creating React components. |
| `cn` from `@/lib/utils` | A utility function for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Card` | A generic card component with rounded corners, a border, background, and shadow. | N/A |
| `CardHeader` | A header section for the card, styled with padding and vertical spacing. | N/A |
| `CardTitle` | A title within the card, styled for emphasis. | N/A |
| `CardDescription` | A description within the card, styled with muted text. | N/A |
| `CardContent` | The main content area of the card, with padding. | N/A |
| `CardFooter` | A footer section for the card, styled with padding and centered items. | N/A |

#### Notes

- All components use `React.forwardRef` to forward refs to the underlying HTML elements, which is useful for accessing DOM nodes directly.
- The `cn` function is used to conditionally apply class names, ensuring that styles are applied correctly.
- Each component has a `displayName` property set for better debugging and React DevTools experience.

### src/components/ui/progress.tsx

#### Overview

# `src/components/ui/progress.tsx`

#### Module Overview

This file defines a `Progress` component that visually represents the progress of a task. It uses Radix UI's `ProgressPrimitive` for the underlying logic and React for the React-specific functionality.

#### Dependencies

| Import | Source | Purpose |
| --- | --- | --- |
| `React` | `react` | Core library for building the component. |
| `ProgressPrimitive` | `@radix-ui/react-progress` | Radix UI's primitive progress component. |
| `cn` | `@/lib/utils` | Utility function for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Progress` | Custom progress component | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Progress` | `className`, `value`, `...props` | `JSX.Element` | Forwards ref and props to `ProgressPrimitive.Root` with custom styling and indicator. |

#### Configuration

None

#### Notes

- The `Progress` component forwards all props to `ProgressPrimitive.Root` and applies custom styles.
- The `ProgressPrimitive.Indicator` component is styled with a gradient background and transitions.
- The `value` prop determines the percentage of progress, influencing the `transform` style of the indicator.

### src/components/ui/scroll-area.tsx

#### Overview

# `src/components/ui/scroll-area.tsx`

#### Module Overview

This file defines two React components, `ScrollArea` and `ScrollBar`, for creating scrollable areas with customizable scrollbars. It leverages the `@radix-ui/react-scroll-area` library for the core scroll area functionality and includes custom styling and forwarding of refs.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Standard React library for building components. |
| `ScrollAreaPrimitive` | Radix UI component for the base scroll area functionality. |
| `cn` | Utility function for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ScrollArea` | A scrollable area component. | N/A |
| `ScrollBar` | A scrollbar component for the scroll area. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ScrollArea` | `className`, `children`, `...props` | JSX Element | Forwards ref and props to `ScrollAreaPrimitive.Root`, includes a `Viewport`, `ScrollBar`, and `ScrollAreaPrimitive.Corner`. |
| `ScrollBar` | `className`, `orientation`, `...props` | JSX Element | Forwards ref and props to `ScrollAreaPrimitive.ScrollAreaScrollbar`, includes a `ScrollAreaPrimitive.ScrollAreaThumb`. |

#### Configuration

None.

#### Notes

- The `ScrollArea` component wraps the `ScrollAreaPrimitive.Root` and includes a `ScrollBar` and `ScrollAreaPrimitive.Corner`.
- The `ScrollBar` component is customizable via `orientation` prop, defaulting to vertical.
- The `cn` utility is used for conditionally applying class names.
- The `ScrollArea` and `ScrollBar` components forward refs to their respective Radix UI components for full integration with React's ref system.

### src/components/ui/separator.tsx

#### Overview

# src/components/ui/separator.tsx

#### Module Overview

This file defines a `Separator` component that renders a customizable horizontal or vertical separator line. It leverages Radix UI's `SeparatorPrimitive` for the underlying logic and styling.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Standard React library for building UI components. |
| `SeparatorPrimitive` | Radix UI primitive component for separators. |
| `cn` | Utility function for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Separator` | Custom separator component that wraps `SeparatorPrimitive.Root`. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `React.forwardRef` | `className`, `orientation`, `decorative`, `...props` | `React.ElementRef<typeof SeparatorPrimitive.Root>` | Forwards ref to `SeparatorPrimitive.Root` and adds custom props. |

#### Configuration

No configurable constants or configuration options are defined in this file.

#### Notes

- The `Separator` component accepts all props of `SeparatorPrimitive.Root` and adds `orientation` and `decorative` props for customization.
- The `cn` utility is used to conditionally apply class names based on the `orientation` prop.
- The `decorative` prop defaults to `true`, meaning the separator will have a default styling suitable for decorative purposes.
- The `orientation` prop can be either `"horizontal"` or `"vertical"`, affecting the separator's dimensions and layout.

### src/components/ui/skeleton.tsx

#### Overview

# Module Overview

This file defines a `Skeleton` component, a lightweight placeholder used to indicate where content will eventually appear. It's useful for improving perceived performance by providing immediate feedback to users while content is loading.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `cn` | Utility function for conditionally combining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Skeleton` | `className`, `...props` | `JSX.Element` | Renders a `div` with animation and background styles to simulate loading content. |

#### Notes

- The `Skeleton` component uses the `cn` utility to combine class names conditionally.
- The `className` prop allows for additional custom styling.
- This component is purely visual and does not affect functionality beyond its visual representation.

### src/components/ui/textarea.tsx

#### Overview

# `src/components/ui/textarea.tsx`

This file defines a reusable `Textarea` component for our UI library. It encapsulates the HTML `<textarea>` element with predefined styles and behavior, making it easy to use across the application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Provides the core React library for building the component. |
| `cn` | Utility function for conditionally joining class names. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Textarea` | A forward-referenced React component for `<textarea>`. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `React.forwardRef` | `React.TextareaHTMLAttributes<HTMLTextAreaElement>`, `React.Ref<HTMLTextAreaElement>` | `React.ForwardedComponent<HTMLTextAreaElement>` | Creates a React forward ref component that wraps the `<textarea>` element with predefined styles and behavior. |

#### Notes

- The `Textarea` component uses the `cn` utility to combine class names, ensuring that custom classes passed via props are merged with the default styles.
- The `ref` prop is forwarded to the `<textarea>` element, allowing it to be easily controlled in parent components.
- The component applies a set of predefined styles for consistent appearance across the application.
- The `disabled` state is handled to prevent interaction and visually indicate the state of the textarea.

### src/components/ui/toaster.tsx

#### Overview

# `src/components/ui/toaster.tsx`

#### Module Overview

This file defines a React component for displaying toast notifications using the `sonner` library. It provides a customizable toast notification system that can be used throughout the application to inform users of various events.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Toaster as Sonner` | The `sonner` library for toast notifications. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Sonner` | The toast notification component from `sonner`. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Toaster` | None | JSX.Element | Returns a `Sonner` component with predefined theme and styling options. |

#### Configuration

The `Toaster` component uses the `sonner` library with the following configuration:

- `theme="dark"`: Sets the toast theme to dark mode.
- `className="toaster group"`: Adds custom CSS classes for styling.
- `toastOptions`: Defines custom CSS classes for different parts of the toast notification.

#### Notes

- The `toastOptions` configuration includes custom styles for the toast, description, action button, and cancel button.
- This component should be used within any React component that requires toast notifications.
- Ensure that the `sonner` library is installed in the project to use this component.

### src/events/CreateEventModal.tsx

#### Overview

# `src/events/CreateEventModal.tsx`

#### Module Overview

This file defines the `CreateEventModal` component, a two-step wizard for creating a new campus event. It allows users to input event details and select assets to generate.

#### Dependencies

- `useState`, `useCallback`, `useMemo` from `react`: State and memoization hooks.
- `motion`, `AnimatePresence` from `framer-motion`: Animation utilities.
- Icons from `lucide-react`: SVG icons for UI elements.
- `Button`, `Textarea` from `@/components/ui`: UI components.
- `createBlankEvent` from `./eventStore`: Function to create a new event object.
- Types from `@/types/campusos`: Type definitions for events and assets.

#### Classes

None.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `CreateEventModal` | `isOpen: boolean`, `onClose: () => void`, `onSubmit: (event: CampusEvent, selectedTypes: AssetType[]) => void` | `React.ReactElement` | Renders the create event modal. |

#### Configuration

### Form Fields
| Key | Label | Placeholder | Icon | Required | Multiline | Hint |
| --- | --- | --- | --- | --- | --- | --- |
| `name` | Event Name | e.g., TechNova 2026 | <Sparkles /> | true |  | The main title that appears on all assets |
| `date` | Date & Time | e.g., March 15-17, 2026 · 9 AM onwards | <CalendarDays /> | true |  |  |
| `venue` | Venue | e.g., Main Auditorium, Block A | <MapPin /> | true |  |  |
| `organizer` | Organizer / Club | e.g., Computer Science Department | <Users /> | true |  |  |
| `theme` | Visual Theme | e.g., Cyberpunk, Neon, Minimalist | <Palette /> | false |  | Sets the visual mood across poster, page & slides |
| `description` | What's this event about? | A short description — what makes this event exciting? (1-3 sentences) | <FileText /> | true | true |  |

### Asset Options
| Type | Label | Icon | Color | Description | Time Estimate |
| --- | --- | --- | --- | --- | --- |
| `poster` | Event Poster | <Image /> | from-pink-500/20 to-orange-500/20 border-pink-500/30 | A striking visual poster for promotions and social media | ~15 sec |
| `landing` | Landing Page | <Globe /> | from-blue-500/20 to-cyan-500/20 border-blue-500/30 | A full event website with details, schedule & registration | ~20 sec |
| `presentation` | Pitch Deck | <Presentation /> | from-purple-500/20 to-indigo-500/20 border-purple-500/30 | Slide deck for sponsors, faculty, or team presentations | ~25 sec |

#### Notes

- The modal uses a two-step process: Event Details → Choose Assets.
- Form validation occurs in `validateStep1`.
- Asset selection is managed via `toggleType`.
- The modal resets state on close or successful submission.

### src/events/EventAssetGroup.tsx

#### Overview

# Module Overview
Displays a polished, card-based UI for grouped event assets with animated progress indicators.

#### Dependencies

- `useMemo` from `react`: For memoizing derived state.
- `motion`, `AnimatePresence` from `framer-motion`: For animations.
- Various icons from `lucide-react`: For visual elements.
- `Button` from `@/components/ui/button`: For interactive elements.
- Types from `@/types/campusos`: For type safety.

#### Classes

None.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `StatusBadge` | `{ status: string }` | `React.ReactNode` | Renders a status badge with icon and text based on the provided status. |
| `EventAssetGroup` | `EventAssetGroupProps` | `React.ReactNode` | Main component rendering the event asset group display. |

#### Configuration

- `assetMeta`: Array of objects defining metadata for each asset type, including label, icon, gradient, glow color, and description.

#### Constants

None.

#### Notes

- The `EventAssetGroup` component uses `AnimatePresence` to handle staggered animations for asset cards.
- The `StatusBadge` component maps status strings to visual configurations.
- The `assetMeta` configuration defines the visual and textual representation of each asset type.
- The component relies on the `assetStatus`, `assetUrls`, and `errors` props to determine the state and content of each asset card.

### src/events/eventStore.ts

#### Overview

# Module Overview

The `src/events/eventStore.ts` file provides functionality to store, retrieve, and manipulate events in the local storage. It ensures that events are persistently stored and easily accessible for the application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `CampusEvent`, `AssetType` from `@/types/campusos` | Type definitions for events and asset types. |
| `uuidv4` from `uuid` | Generates unique identifiers for new events. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `getEvents()` | None | `CampusEvent[]` | Retrieves all events from local storage. |
| `getEventById(id: string)` | `id` of type `string` | `CampusEvent | null` | Retrieves a specific event by its ID. |
| `saveEvent(event: CampusEvent)` | `event` of type `CampusEvent` | `void` | Saves or updates an event in local storage. |
| `updateEventAssets(eventId: string, assetType: AssetType, assetId: string)` | `eventId` of type `string`, `assetType` of type `AssetType`, `assetId` of type `string` | `void` | Updates the assets of a specific event. |
| `deleteEvent(id: string)` | `id` of type `string` | `void` | Deletes an event by its ID. |
| `clearAllEvents()` | None | `void` | Clears all events from local storage. |
| `createBlankEvent(overrides?: Partial<CampusEvent>)` | `overrides` of type `Partial<CampusEvent>` (optional) | `CampusEvent` | Creates a new blank event with optional overrides. |

#### Notes

- The `localStorage` API is used for persistence, which has a limited storage capacity. Consider handling storage limits gracefully in production.
- Error handling is minimal; in a production environment, consider more robust error handling and user feedback mechanisms.
- The `createBlankEvent` function generates a new event with a unique ID using `uuidv4`, ensuring each event is distinct.

### src/events/index.ts

#### Overview

# src/events/index.ts

#### Module Overview

This file consolidates and exports various event-related components and functions from the `events` directory. It provides a centralized entry point for accessing event creation, management, and asset handling functionalities.

#### Dependencies

- **CreateEventModal**: A component for creating new events.
- **EventAssetGroup**: A component for managing event assets.
- **useEventGenerator**: A custom hook for generating events.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| CreateEventModal | Component for creating new events | `render()` |
| EventAssetGroup | Component for managing event assets | `addAsset()`, `removeAsset()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `getEvents` | None | Array of events | Retrieves all stored events |
| `getEventById` | `eventId: string` | Event object | Retrieves a single event by ID |
| `saveEvent` | `event: Event` | `void` | Saves a new event |
| `updateEventAssets` | `eventId: string`, `assets: Asset[]` | `void` | Updates the assets for a given event |
| `deleteEvent` | `eventId: string` | `void` | Deletes an event by ID |
| `clearAllEvents` | None | `void` | Clears all events |
| `createBlankEvent` | None | `Event` | Creates a new blank event |

#### Notes

- Ensure to handle errors appropriately when calling these functions, especially when dealing with asynchronous operations.
- The `Event` and `Asset` types should be imported from the appropriate modules.
- The `useEventGenerator` hook is useful for dynamic event creation within functional components.

### src/events/useEventGenerator.ts

#### Overview

# `src/events/useEventGenerator.ts`

Manages the generation of event assets (poster, landing page, presentation) for a `CampusEvent`.

#### Dependencies

- `useState`, `useCallback` from `react`
- `toast` from `sonner`
- `generate` from `@/services/api`
- `saveAsset` from `@/services/assetRegistry`
- `saveEvent`, `updateEventAssets` from `./eventStore`
- Types: `CampusEvent`, `AssetType`, `EventGenerationStatus` from `@/types/campusos`

#### Functions

| Function                 | Parameters                              | Returns                                                                                             | Description                                                                                     |
|--------------------------|-----------------------------------------|-----------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------|
| `buildPosterPrompt`      | `event: CampusEvent`                    | `string`                                                                                            | Constructs a prompt for generating a poster based on the event details.                         |
| `buildLandingPrompt`     | `event: CampusEvent`                    | `string`                                                                                            | Constructs a prompt for generating a landing page based on the event details.                   |
| `buildPresentationPrompt`| `event: CampusEvent`                    | `string`                                                                                            | Constructs a prompt for generating a presentation based on the event details.                   |
| `getPromptForType`       | `event: CampusEvent`, `assetType: AssetType` | `string`                                                                                            | Returns the appropriate prompt based on the asset type.                                         |
| `useEventGenerator`      | None                                    | `{ event: CampusEvent | null, overallStatus: OverallStatus, assetStatus: EventGenerationStatus, assetUrls: Partial<Record<AssetType, string>>, errors: Partial<Record<AssetType, string>>, selectedTypes: AssetType[], setSelectedTypes: (types: AssetType[]) => void, toggleAssetType: (type: AssetType) => void, runEventGeneration: (event: CampusEvent, typesToGenerate?: AssetType[]) => Promise<void>, reset: () => void }` | React hook that manages the state and functions for generating event assets.                    |

#### Notes

- Asset generation runs sequentially. If one asset fails, the process continues to the next.
- The `overallStatus` is set to "done" if at least one asset is successfully generated, otherwise "error".
- Ensures at least one asset type is always selected to prevent invalid states.
- Uses `toast` for user notifications on success or failure of asset generation.

### src/hooks/useCreationEngine.ts

#### Overview

# useCreationEngine.ts

#### Module Overview

This file defines the `useCreationEngine` hook, which manages the state and logic for creating new assets within the application. It handles the creation process, updates the session state, and communicates with external services to generate and save the assets.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `useState`, `useCallback` from `react` | React hooks for managing state and memoizing functions. |
| `toast` from `sonner` | Utility for displaying notifications. |
| `generate` from `@/services/api` | Function to generate assets based on a prompt. |
| `saveAsset` from `@/services/assetRegistry` | Function to save generated assets to the registry. |
| `CreationSession`, `AssetType` from `@/types/campusos` | Type definitions for creation sessions and asset types. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `useCreationEngine` | None | `{ session: CreationSession, runCreation: (prompt: string, assetType?: AssetType) => Promise<void>, reset: () => void }` | Hook that provides the creation engine's state and functions. |

#### Notes

- The `runCreation` function updates the session state to reflect the current status of the creation process.
- The `reset` function restores the session to its initial state.
- Error handling within `runCreation` updates the session state to indicate an error and displays a notification.
- The `generate` function is called with the user's prompt and the selected asset type.
- The `saveAsset` function is responsible for persisting the generated asset in the registry.
- Notifications are shown using the `toast` utility to inform the user of the creation process's outcome.

### src/lib/utils.ts

#### Overview

# Module Overview

This file defines utility functions for class name manipulation, specifically for merging and combining class names using `clsx` and `tailwind-merge`. It aims to simplify the process of constructing complex class names in a clean and maintainable way.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `clsx` | Utility for combining class names. |
| `tailwind-merge` | Utility for merging Tailwind CSS classes. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `cn` | `...inputs: ClassValue[]` | `string` | Combines class names using `clsx` and merges them with `tailwind-merge`. |

#### Notes

- The `cn` function is a convenient wrapper around `clsx` and `tailwind-merge`, making it easier to manage complex class name combinations.
- Always ensure that all inputs to `cn` are valid class names or strings to avoid unexpected behavior.

### src/pages/Create.tsx

#### Overview

# `src/pages/Create.tsx` Documentation

#### Module Overview

This file defines the `Create` page component, which allows users to select an asset type and input a prompt for AI-driven content creation. It handles the UI for choosing asset types, toggling between free and guided prompt modes, and initiating the creation process.

#### Dependencies

- `useState` from `react`: Manages local component state.
- `motion`, `AnimatePresence` from `framer-motion`: Provides animations for UI elements.
- Various icons from `lucide-react`: Used for visual elements in the UI.
- `Button`, `IntentInput`, `PromptTemplateSelector` from local components: UI components for buttons, input fields, and prompt templates.
- `CreateEventModal`, `EventAssetGroup`, `useEventGenerator` from local events module: Handles event-related UI and logic.
- `useCreationEngine` from local hooks: Provides the creation engine logic and session management.
- Type definitions from local types: Defines the shape of data used in the component.

#### Classes

None.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Create` | None | `React.FC` | Main component function that renders the Create page. |

#### Configuration

- `assetTypes`: An array of objects defining available asset types, their labels, icons, descriptions, and styles.

#### Constants

- `PromptMode`: A type defining the possible prompt modes (`"free"` or `"guided"`).

#### Notes

- The component uses `framer-motion` for animations, which adds a dynamic feel to the UI.
- The `useCreationEngine` hook is central to the creation process, managing the session and providing methods to run and reset the creation.
- The `useEventGenerator` hook is used specifically for handling event-related asset creation.
- The `Create` component conditionally renders different UI elements based on the creation session status and whether event mode is active.
- The `assetTypes` array is used to render a grid of selectable asset types, each with unique styling and descriptions.
- The `PromptMode` type is used to toggle between free and guided input modes for the user.

### src/pages/History.tsx

#### Overview

# Module Overview
Displays a list of generated assets with options to view, download, and clear the list.

#### Dependencies

- `useState`, `useEffect` from `react`: Core React hooks for state and side effects.
- `motion`, `AnimatePresence` from `framer-motion`: Animation library for smooth transitions.
- Icons from `lucide-react`: Icon components for UI elements.
- `Card`, `CardContent`, `Button`, `Badge` from `@/components/ui`: UI components for layout and interaction.
- `getAllAssets`, `clearAllAssets`, `getAssetById` from `@/services/assetRegistry`: Service functions to interact with asset data.
- `toast` from `sonner`: Notification system for user feedback.

#### Functions

| Function | Parameters | Returns | Description |
|-|-|-|-|
| `relativeTime` | `dateStr: string` | `string` | Converts a date string to a relative time format. |
| `History` | None | `React.FC` | Main component rendering the history page. |

#### Configuration

| Config | Type | Description |
|-|-|-|
| `typeConfig` | `Record<AssetType, { icon: React.ReactNode; label: string; color: string; gradient: string; accentBar: string }>` | Configuration for different asset types, including icons, labels, and styles. |

#### Notes

- The `History` component relies on the `assetRegistry` service for data.
- Animations are used extensively for a smooth user experience.
- User notifications are handled via the `toast` function from `sonner`.
- Ensure asset data is correctly managed to avoid inconsistencies in the UI.

### src/pages/Home.tsx

#### Overview

# `src/pages/Home.tsx`

#### Module Overview

Renders the homepage, showcasing the product's value proposition, workflow, and capabilities. Uses animations and visual elements to engage users.

#### Dependencies

- `framer-motion`: Animations
- `react-router-dom`: Navigation
- `lucide-react`: Icons
- `@/components/ui/button`: Reusable button component
- `@/components/ui/card`: Reusable card component

#### Constants

| Constant | Type | Description |
| --- | --- | --- |
| `features` | `Array` | Array of objects describing the product's workflow |
| `creationTypes` | `Array` | Array of objects describing different types of creations |

#### Notes

- Uses Tailwind CSS for styling
- Relies on global styles defined elsewhere (`noise-overlay`, `aurora-orb`, etc.)
- `motion` components from `framer-motion` are used extensively for animations
- `Card` and `CardContent` are custom components imported from `@/components/ui`

### src/pages/NotFound.tsx

#### Overview

# `src/pages/NotFound.tsx`

#### Module Overview

This file defines a React component for rendering a 404 "Page Not Found" error page. It provides a visually appealing message to users when they navigate to a non-existent route within the application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | From `react-router-dom`, used for navigation between routes. |
| `motion` | From `framer-motion`, used for animations. |
| `Home` | From `lucide-react`, used as an icon. |
| `Button` | Custom button component from `@/components/ui/button`. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `NotFound` | None | JSX element | Renders the 404 page with an animated message and a button to navigate back to the home page. |

#### Notes

- The `NotFound` component uses `framer-motion` for smooth animations when the page is loaded.
- The `Button` component includes an icon and text, styled to stand out.
- The `Link` component navigates users back to the home page when the button is clicked.
- Ensure that the `react-router-dom` and `framer-motion` libraries are correctly installed and configured in your project.

### src/pages/viewers/LandingPageViewer.tsx

#### Overview

# LandingPageViewer.tsx

#### Module Overview

This file defines the `LandingPageViewer` component, which displays a landing page based on an asset ID fetched from the URL. It includes functionality for downloading the HTML content and copying the deploy-ready code.

#### Dependencies

| Import | Description |
| --- | --- |
| `useEffect`, `useState` | React hooks for managing side effects and state. |
| `useParams` | React Router hook to extract URL parameters. |
| `Download`, `FileText`, `Copy`, `Check`, `ArrowLeft` | Icons from `lucide-react`. |
| `getAssetById` | Function from `assetRegistry` to fetch an asset by ID. |
| `toast` | Function from `sonner` for displaying notifications. |
| `GeneratedAssetV2` | Type definition for the asset from `campusos` types. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `downloadHTML` | None | None | Downloads the HTML content of the asset as a file. |
| `copyCode` | None | None | Copies the HTML content of the asset to the clipboard. |

#### Notes

- The `useEffect` hook fetches the asset data when the `id` changes.
- The `iframe` element renders the content of the asset.
- The component shows a loading spinner while fetching the asset.
- If the asset is not found, it displays a "Not Found" message.
- The floating toolbar provides options to download the HTML and copy the code.
- The toolbar can be hidden and shown using a button.

### src/pages/viewers/PosterViewer.tsx

#### Overview

# PosterViewer.tsx

#### Module Overview

This file defines the `PosterViewer` component, which displays a poster based on an asset ID and provides options to download the poster as HTML or export it as a PDF.

#### Dependencies

| Import | Description |
| --- | --- |
| `useEffect`, `useState`, `useRef` | React hooks for managing state and side effects. |
| `useParams` | React Router hook to access route parameters. |
| `Download`, `FileText`, `ArrowLeft` | Icons from `lucide-react`. |
| `getAssetById` | Function from `assetRegistry` to fetch an asset by ID. |
| `html2canvas` | Library to capture HTML content as an image. |
| `jsPDF` | Library to generate PDF files. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `downloadHTML` | None | None | Downloads the poster content as an HTML file. |
| `downloadPDF` | None | None | Exports the poster content as a PDF file. |

#### Notes

- The `PosterViewer` component relies on the `id` parameter from the route to fetch and display the poster.
- The `downloadPDF` function uses `html2canvas` to capture the poster content as an image and then converts it to a PDF using `jsPDF`.
- Error handling is minimal; consider adding user feedback for failed downloads.
- The `posterRef` is used to reference the poster container for PDF export. Ensure it is attached to the correct element.

### src/pages/viewers/PresentationViewer.tsx

#### Overview

# src/pages/viewers/PresentationViewer.tsx

#### Module Overview

Displays a presentation with various slide types, handles navigation, and allows downloading as PDF.

#### Dependencies

- `useEffect, useState, useCallback` from `react`: React hooks for managing state and side effects.
- `useParams` from `react-router-dom`: Extracts parameters from the URL.
- `Download, ArrowLeft, ChevronLeft, ChevronRight` from `lucide-react`: Icons used in the UI.
- `getAssetById` from `@/services/assetRegistry`: Fetches presentation data by ID.
- Types `GeneratedAssetV2, PresentationData, Slide` from `@/types/campusos`: Type definitions for assets and slides.
- `jsPDF`: Library for generating PDF documents.

#### Functions

| Function        | Parameters            | Returns | Description                                                                                           |
|-----------------|-----------------------|---------|-------------------------------------------------------------------------------------------------------|
| `SlideContent`  | `{ slide: Slide }`    | JSX.Element | Renders the content of a slide based on its type.                                                     |

#### Notes

- Slide types include `title`, `content`, `image-focus`, `stats`, `two-column`, `closing`, `quote`, and `team`.
- Animations are applied to slide elements for a smooth transition effect.
- Keyboard navigation is supported using ← → arrow keys.
- PDF download functionality is available via the `Download` icon.

### src/prompt-intelligence/PromptTemplateSelector.tsx

#### Overview

# `src/prompt-intelligence/PromptTemplateSelector.tsx`

#### Module Overview

This file contains the `PromptTemplateSelector` component, which allows users to select a template, fill out required and optional fields, and generate a prompt. It includes template cards for selection, a form for field input, and a live preview of the generated prompt.

#### Dependencies

- `react`: Core React library for building UI components.
- `framer-motion`: Animation library for smooth transitions.
- `lucide-react`: Icon library for various UI elements.
- `@/components/ui/button` and `@/components/ui/textarea`: Custom UI components for buttons and textareas.
- `@/types/campusos`: Type definitions for asset types.
- `./promptTemplates`: Module containing prompt templates and related functions.
- `./buildPrompt`: Module for building and validating prompts.

#### Classes

None

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `PromptTemplateSelector` | `assetType: AssetType | "auto"`, `onPromptReady: (prompt: string) => void`, `isLoading?: boolean` | `React.ReactElement` | Renders the template selector component. |

#### Configuration

None

#### Constants

| Constant | Type | Description |
| --- | --- | --- |
| `assetIcons` | `Record<AssetType, React.ReactNode>` | Mapping of asset types to their respective icons. |
| `assetColors` | `Record<AssetType, { bg: string; text: string }>` | Mapping of asset types to their respective background and text colors. |
| `assetLabel` | `Record<AssetType, string>` | Mapping of asset types to their respective labels. |

#### Notes

- The component uses `useMemo` to derive various states based on the selected template and form values.
- The `useEffect` hooks handle side effects like setting the default template and scrolling to the form.
- The `handleSubmit` function checks if the form is valid before calling `onPromptReady` with the generated prompt.
- The component renders template cards, a form for field input, and a live preview of the generated prompt.
- The `renderField` function dynamically renders required and optional fields based on the selected template.

### src/prompt-intelligence/buildPrompt.ts

#### Overview

# Module Overview

This file contains functions to build, validate, and enhance prompts for the CampusOS Prompt Intelligence Layer. It ensures that prompts are clean, complete, and adhere to specified tone and length constraints.

#### Dependencies

- `PromptTemplate` and `PromptField` from `./promptTemplates`: Define the structure and metadata for prompts and their fields.

#### Functions

| Function                      | Parameters                                                                                       | Returns                                                                                              | Description                                                                                       |
|-------------------------------|--------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| `cleanFieldValue`             | `value: string | undefined`                                                                 | `string`                                                                                             | Cleans and normalizes a field value.                                                              |
| `hasValue`                    | `value: string | undefined`                                                                 | `boolean`                                                                                            | Checks if a field has a meaningful value.                                                         |
| `filterEmptyOptionalFields`   | `values: Record<string, string>, fields: PromptField[]`                                         | `Record<string, string>`                                                                             | Filters out empty optional fields from the values object.                                         |
| `validateRequiredFields`      | `values: Record<string, string>, fields: PromptField[]`                                         | `{ valid: boolean; missingFields: string[] }`                                                        | Validates that all required fields have values.                                                   |
| `getToneModifier`             | `tone: BuildPromptOptions["tone"]`                                                              | `string`                                                                                             | Gets tone modifier text based on the selected tone.                                               |
| `buildPrompt`                 | `template: PromptTemplate, values: Record<string, string>, options: BuildPromptOptions = {}`     | `string`                                                                                             | Builds an optimized prompt string from template and form values.                                  |
| `buildPromptPreview`          | `template: PromptTemplate, values: Record<string, string>`                                       | `{ preview: string; isComplete: boolean; missingCount: number }`                                      | Generates a preview of what the prompt will look like, showing placeholders for empty required fields. |
| `getFormCompleteness`         | `values: Record<string, string>, fields: PromptField[]`                                         | `{ percentage: number; filledRequired: number; totalRequired: number; filledOptional: number; totalOptional: number }` | Calculates the completeness percentage of filled fields.                                          |
| `enhanceFreePrompt`           | `prompt: string, assetType?: string`                                                             | `string`                                                                                             | Enhances a free-form prompt with standard CampusOS quality modifiers.                              |

#### Configuration

- `DEFAULT_OPTIONS`: Default options for building prompts.
  ```typescript
  const DEFAULT_OPTIONS: BuildPromptOptions = {
      enforceTone: true,
      tone: "student-friendly",
      maxLength: 2000,
  };
  ```

#### Notes

- The `buildPrompt` function truncates the prompt with "..." if it exceeds the maximum length.
- The `enhanceFreePrompt` function adds quality hints based on the asset type, but only if the prompt is shorter than 500 characters.

### src/prompt-intelligence/index.ts

#### Overview

# src/prompt-intelligence/index.ts

#### Module Overview

This file serves as the entry point for the `prompt-intelligence` module, aggregating and exporting key functionalities related to prompt templates and prompt building. It centralizes access to various prompt-related utilities and templates, making it easier to manage and extend the prompt logic across the application.

#### Dependencies

This file imports and exports several components from other modules within the `prompt-intelligence` directory, providing a cohesive interface for working with prompt templates and building prompts.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| PromptTemplateSelector | Not explicitly defined in this file. Imported from `./PromptTemplateSelector`. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `buildPrompt` | `BuildPromptOptions` | `string` | Constructs a complete prompt based on provided options. |
| `buildPromptPreview` | `BuildPromptOptions` | `string` | Generates a preview version of the prompt. |
| `validateRequiredFields` | `PromptTemplate`, `object` | `boolean` | Checks if all required fields are present in the given template. |
| `getFormCompleteness` | `PromptTemplate`, `object` | `number` | Calculates the completeness score of the form based on the provided data. |
| `filterEmptyOptionalFields` | `object` | `object` | Filters out empty optional fields from the given object. |
| `enhanceFreePrompt` | `string`, `PromptTemplate` | `string` | Enhances a free-form prompt with context from a template. |

#### Configuration or Constants

| Identifier | Type | Description |
| --- | --- | --- |
| `promptTemplates` | `object` | Collection of predefined prompt templates. |
| `getTemplatesByAssetType` | `function` | Retrieves templates by asset type. |
| `getTemplateById` | `function` | Retrieves a template by its ID. |
| `getDefaultTemplateForAssetType` | `function` | Retrieves the default template for a given asset type. |

#### Notes

- Ensure that all required fields are validated before using `buildPrompt` or `buildPromptPreview`.
- The `enhanceFreePrompt` function is useful for adding context to user-generated prompts.
- The `getFormCompleteness` function provides a numerical score indicating how complete the form is, which can be used for UX feedback.

### src/prompt-intelligence/promptTemplates.ts

#### Overview

# Module Overview
This file defines templates for generating prompts used by the CampusOS Prompt Intelligence Layer. These templates structure the input data required to create various types of assets like posters, landing pages, and presentations.

# Dependencies
- `AssetType` from `@/types/campusos`: Defines the types of assets that can be generated (e.g., poster, landing page, presentation).

# Interfaces
| Interface | Purpose |
| --- | --- |
| `PromptField` | Represents a single field in a prompt template. Contains metadata like key, label, placeholder, and whether it's required. |
| `PromptTemplate` | Represents a complete prompt template. Contains an ID, label, description, asset type, fields, and a function to generate the base prompt. |

# Templates
| Template | Asset Type | Description |
| --- | --- | --- |
| `posterEventTemplate` | `poster` | Template for generating event posters. |
| `posterClubRecruitmentTemplate` | `poster` | Template for generating club recruitment posters. |
| `landingClubTemplate` | `landing` | Template for generating club landing pages. |
| `landingEventTemplate` | `landing` | Template for generating event landing pages. |
| `presentationPitchTemplate` | `presentation` | Template for generating startup pitch presentations. |

# Notes
- Each template's `basePrompt` function constructs a string prompt based on the provided field values.
- Optional fields are handled gracefully within each template's `basePrompt` function.
- The `multiline` property in `PromptField` indicates whether a field accepts multiple lines of input.

### src/services/api.ts

#### Overview

# `src/services/api.ts`

#### Module Overview

This file defines a set of utility functions for making HTTP POST requests to the API endpoints. It abstracts the fetch logic, handling JSON parsing and error management, to simplify API interactions across the application.

#### Dependencies

- `@/types/campusos`: Provides type definitions for generated assets and asset types.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `post<T>` | `endpoint: string`, `body: unknown` | `Promise<T>` | Makes a POST request to the specified endpoint with the provided body. Returns parsed JSON or throws an error on failure. |
| `generate` | `prompt: string`, `assetType: AssetType | "auto" = "auto"` | `Promise<GeneratedAssetV2>` | Calls the `/generate` endpoint with a prompt and asset type, returning a generated asset. |

#### Notes

- `post<T>` handles HTTP errors by parsing the response and throwing an error with a descriptive message.
- The `generate` function uses `post<T>` to interact with the `/generate` endpoint, simplifying its usage across the application.
- The `assetType` parameter in `generate` defaults to `"auto"` if not provided.

### src/services/assetRegistry.ts

#### Overview

# src/services/assetRegistry.ts

#### Module Overview

This file manages the local storage of asset data and metadata, providing functions to save, retrieve, delete, and clear assets from the local storage.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `GeneratedAssetV2`, `AssetRegistryEntry` | Types for asset data and registry entries |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `saveAsset` | `asset: GeneratedAssetV2` | `void` | Saves the full asset data and a lightweight metadata entry to local storage. |
| `getAllAssets` | `none` | `AssetRegistryEntry[]` | Retrieves all asset metadata entries from local storage. |
| `getAssetById` | `id: string` | `GeneratedAssetV2 | null` | Retrieves the full asset data by its ID from local storage. |
| `deleteAsset` | `id: string` | `void` | Deletes an asset by its ID from local storage. |
| `clearAllAssets` | `none` | `void` | Clears all assets and related data from local storage. |

#### Notes

- The local storage is limited in size, so the registry keeps only the latest 100 entries.
- Error handling is minimal; localStorage operations are generally idempotent and safe to ignore failures.
- This module does not handle synchronization with a remote server; it only manages local storage.

### src/types/campusos.ts

#### Overview

# `src/types/campusos.ts`

This file defines TypeScript types and interfaces used throughout the CampusOS project. It standardizes the structure of various data entities, ensuring consistency and type safety across the application.

#### Dependencies

| Import | Purpose |
|--------|---------|
| None | This file does not import any external modules. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| None | This file does not define any classes. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| None | This file does not define any functions. |

#### Types

| Type | Purpose |
|------|---------|
| `IntentType` | Defines possible intent types for content creation. |
| `AssetType` | Defines possible asset types generated by the system. |
| `Tone` | Defines possible tones for content. |
| `StepType` | Defines types of steps in a content creation pipeline. |
| `StepStatus` | Defines statuses for steps in a content creation pipeline. |
| `ParsedIntent` | Represents a parsed intent with details for content creation. |
| `TitleSlide` | Represents a title slide in a presentation. |
| `ContentSlide` | Represents a content slide in a presentation. |
| `ImageFocusSlide` | Represents an image focus slide in a presentation. |
| `StatsSlide` | Represents a stats slide in a presentation. |
| `TwoColumnSlide` | Represents a two-column slide in a presentation. |
| `ClosingSlide` | Represents a closing slide in a presentation. |
| `QuoteSlide` | Represents a quote slide in a presentation. |
| `TeamSlide` | Represents a team slide in a presentation. |
| `TimelineSlide` | Represents a timeline slide in a presentation. |
| `Slide` | Union type for all slide types. |
| `PresentationData` | Represents data for a presentation. |
| `GeneratedAssetV2` | Represents a generated asset in the new unified format. |
| `AssetRegistryEntry` | Represents an entry in the asset registry. |
| `CreationSession` | Represents a creation session for generating assets. |
| `CampusEvent` | Represents a campus event. |
| `EventAssetType` | Type for asset types in a campus event. |
| `EventGenerationStatus` | Type for generation statuses of assets in a campus event. |
| `PipelineStep` | Represents a step in a content creation pipeline. |
| `Pipeline` | Represents a content creation pipeline. |
| `GeneratedAsset` | Represents a generated asset from a pipeline step. |
| `HistoryEntry` | Represents an entry in the history of generated assets. |

#### Notes

- The types defined in this file are used to ensure type safety and consistency across the application.
- Legacy types are kept for backwards compatibility with older parts of the system.
- When updating or adding new types, ensure that they are used consistently throughout the codebase to maintain type safety.

